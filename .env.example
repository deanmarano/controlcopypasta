# ControlCopyPasta Environment Variables
# Copy to .env and edit as needed: cp .env.example .env
#
# For local development, you don't need to set anything - sensible defaults
# are built into the dev config. This file is mainly for production deployments.

# ── Production Required ──────────────────────────────────────────────────────
# Generate both with: mix phx.gen.secret
# Or with Docker: docker run --rm elixir:1.18 mix phx.gen.secret
SECRET_KEY_BASE=generate_with_mix_phx_gen_secret
GUARDIAN_SECRET_KEY=generate_with_mix_phx_gen_secret

# Database (only needed for docker-compose.prod.yml)
POSTGRES_PASSWORD=your_secure_password_here

# Your domain (used for URLs in emails and CORS)
PHX_HOST=recipes.yourdomain.com

# ── Production Optional ──────────────────────────────────────────────────────
# Admin access - comma-separated email addresses that can access /admin
ADMIN_EMAILS=admin@example.com

# Frontend URL for magic link emails (defaults to https://<PHX_HOST>)
# FRONTEND_URL=https://recipes.yourdomain.com

# SMTP (required for magic link login to work)
# Without SMTP, users can't log in. In dev, Mailhog catches all emails.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your-smtp-username
# SMTP_PASSWORD=your-smtp-password

# WebAuthn/Passkey (defaults to localhost for dev)
# WEBAUTHN_ORIGIN=https://recipes.yourdomain.com
# WEBAUTHN_RP_ID=recipes.yourdomain.com

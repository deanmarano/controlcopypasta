# Development Procfile - includes infrastructure
# Run with: overmind start -f Procfile.dev
# Or: foreman start -f Procfile.dev

infra: docker compose up
backend: cd backend && mix phx.server
frontend: cd frontend && npm run dev -- --host 0.0.0.0
